@model TestBestHack.Models.Source

@using (Html.BeginForm("Index", "Home",FormMethod.Post))
{
<div>
    @Html.LabelFor(i=>i.Category)
    @Html.EnumDropDownListFor(i=>i.Category, new { @class= "form-control dropdown" })
</div>
    <br />
<button type="submit" class="btn btn-primary">Save</button>
}